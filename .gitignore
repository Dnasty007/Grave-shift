node_modules/
dist/
index.mjs

# Open-source browser client (clone via npm run play-local); large monorepo — not committed
hytopia-client/

# Local Cursor / review tooling (do not publish)
.video-review/
.claude/
debug-agent*.log

# Huge Mineways exports (legacy PlayCanvas tree)
legacy-playcanvas-client/public/maps/*.obj
legacy-playcanvas-client/public/maps/*.glb
legacy-playcanvas-client/public/maps/*.mtl
legacy-playcanvas-client/public/maps/*.png

# Huge map mesh exports — exceed GitHub's 100MB limit; kept local only.
# (Dracula's Castle + Ice Map: ~80MB and ~125MB GLBs + generated .optimized data.)
assets/models/environment/Ice-Map/
assets/models/environment/Draculas-Castle/
assets/ice-map/
assets/draculas-castle/

# Local Hytopia save-state backend (player persistence dev data)
dev/
