# Persistence (The M$ System)
* **PersistenceManager**: Use `PersistenceManager.instance.setPlayerData(playerId, key, value)` to save M$ currency.
* **Local Development**: Saved data is stored in the `/dev/` directory of the project.
* **Global Leaderboards**: Use `setGlobalData` for tracking the "highest wave reached" across the entire server.
