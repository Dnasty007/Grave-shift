# Audio Manager & Stingers
* **AudioManager Singleton**: Use `world.audioManager` to control all active sounds.
* **Dynamic Mixing**: Adjust `playbackSpeed` or `volume` in real-time based on zombie proximity.
* **Easter Egg Songs**: Use `loop: false` for the 3-minute tracks and trigger them via specific world interactions.
